{{> menu}}

<form class="ui form" method="POST">
  <div class="field">
    <label>Title</label>
    <input type="text" name="title" value="{{shark.title}}">
  </div>

  <div class="field">
    <label>Length</label>
    <input type="text" name="length" value="{{shark.length}}">
  </div>

  <button class="ui green button">Update</button>
</form>